import store from '../index';

const state = {
    //data : {}
};

const mutations = {
    /*
    setData(state, value) {
        state.data = value;
    },*/
};

const actions = {
    //setData: ({commit}, value) => commit('setData', value),
};

const getters = {
    //data: state => state.data,
};

export default {
    state,
    getters,
    actions,
    mutations
};